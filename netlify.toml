[build]
  command = "pnpm install && pnpm run build"
  publish = "artifacts/legacyqr/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
